import { create } from '@storybook/theming';

export const theme = create({
  base: 'light',
  brandTitle: "Feras' Starter",
  brandUrl: 'https://github.com/FerasAloudah/feras-starter',
  brandImage: null,
});
